// CRUD operations for article comments

export default async function commentRoute(fastify){
    fastify.get('/:id', async (request, reply) => {

    })

    fastify.delete('/:id', async (request, reply) => {

    })

    fastify.patch('/:id', async (request, reply) => {

    })

    fastify.delete('/:id', async (request, reply) => {
        
    })
}